(function (nx, ui, toolkit, annotation, global) {
    /**
     * @class Image
     * @namespace nxex.struct.tag
     */
    var EXPORT = nx.define("nxex.struct.tag.Image", nxex.struct.Element, {
        view: {
            tag: "img"
        }
    });
})(nx, nx.ui, nxex.toolkit, nxex.annotation, window);
