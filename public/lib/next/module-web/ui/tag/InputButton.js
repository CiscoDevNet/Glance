(function (nx) {
    /**
     * @class InputButton
     * @namespace nx.ui.tag
     */
    var EXPORT = nx.define("nx.ui.tag.InputButton", nx.ui.tag.Input, {
        view: {
            attributes: {
                type: "button"
            }
        }
    });
})(nx);
