(function (nx) {

    var EXPORT = nx.define("nx.ui.tag.Source", nx.ui.Element, {
        methods: {
            init: function () {
                this.inherited("source");
            }
        }
    });
})(nx);
